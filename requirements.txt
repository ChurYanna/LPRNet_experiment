# pip install -r requirements.txt

# LPRNet-only 最小依赖（用于课程作业的训练/评测/数据裁剪）
numpy>=1.18.5
opencv-python>=4.1.2
torch>=1.7.0
tqdm>=4.41.0

# 可视化（画曲线/表格汇总）
matplotlib>=3.2.2

# 日志（CSV在标准库；TensorBoard可选但建议装）
tensorboard>=2.4.1
